{
  "name": "sop-frontend",
  "version": "0.1.0",
  "description": "Frontend for SOP Web App (HTML/CSS/vanilla JS).",
  "private": true,
  "scripts": {
    "start": "npx serve -l 5173 public",
    "lint": "echo \"(add eslint later)\""
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
